import Button from "./UI/Button";
import "./UserForm.css";

const UserForm = (props) => {
  return (
    <div>
      <p>User Form</p>
      <Button> Add user </Button>
    </div>
  );
};

export default UserForm;
